{
  "name": "three-starter-template",
  "version": "1.0.0",
  "description": "Provides a starter template that incorporates three.js, parcel, typescript and scss.",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && parcel build ./src/html/* --no-cache",
    "start": "npm run clean && parcel ./src/html/* --no-cache"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-glsl": "^2.10.3",
    "@parcel/transformer-sass": "^2.10.3",
    "@types/dat.gui": "^0.7.12",
    "@types/three": "^0.159.0",
    "dat.gui": "^0.7.9",
    "parcel": "^2.10.3",
    "postcss": "^8.4.32",
    "postcss-modules": "^4.3.1",
    "three": "^0.159.0"
  }
}
